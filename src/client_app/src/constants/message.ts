const REQUIRED_MESSAGE = "Required.";
const NAME_EXISTS_MESSAGE = "This name already exists.";
const GREATER_THAN_ZERO_MESSAGE = "Must enter a number greater than 0.";

const DefinedErrorMessage = {
  REQUIRED_MESSAGE,
  NAME_EXISTS_MESSAGE,
  GREATER_THAN_ZERO_MESSAGE,
};

export default DefinedErrorMessage;
